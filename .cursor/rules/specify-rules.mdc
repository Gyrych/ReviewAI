# 10_ReviewAI Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-10-23

## Active Technologies
- Node.js + TypeScript (frontend: React + TypeScript, backend: Node.js 18+, TypeScript 5.x) + `vite`, `react`, `express`, `node-fetch`, `redis` (see `frontend/package.json` & `services/circuit-agent/package.json`) (003-validate-code-against-constitution)
- Node.js >= 18 (运行时)、TypeScript >= 5.x（源码）；前端基于 Vite + React。 + `express`（后端 HTTP）、`multer`（multipart）、`node-fetch`/`OpenRouterClient`（上游调用）、`vite`、`react`、`@playwright/test`（端到端测试）、`vitest`（单元测试）。 (004-audit-constitution)
- 开发/默认：文件系统（`ArtifactStoreFs`）；可选 Redis 用于短期进度/状态（`redis` dependency 已存在）。 (004-audit-constitution)

## Project Structure
```
backend/
frontend/
tests/
```

## Commands
npm test; npm run lint

## Code Style
Node.js + TypeScript (frontend: React + TypeScript, backend: Node.js 18+, TypeScript 5.x): Follow standard conventions

## Recent Changes
- 004-audit-constitution: Added Node.js >= 18 (运行时)、TypeScript >= 5.x（源码）；前端基于 Vite + React。 + `express`（后端 HTTP）、`multer`（multipart）、`node-fetch`/`OpenRouterClient`（上游调用）、`vite`、`react`、`@playwright/test`（端到端测试）、`vitest`（单元测试）。
- 003-validate-code-against-constitution: Added Node.js + TypeScript (frontend: React + TypeScript, backend: Node.js 18+, TypeScript 5.x) + `vite`, `react`, `express`, `node-fetch`, `redis` (see `frontend/package.json` & `services/circuit-agent/package.json`)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
